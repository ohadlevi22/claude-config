# Nvidia domain — placeholder

Replace this primer when you activate this pack for real work.

## Codebase

Set `codebasePath` in `domain.json` to the product repo root before running (coding + scorers verify against that tree).

## What to put here

- Product / org context for the codebase you will code against
- Main services and non-negotiable invariants
- Pointers to deeper internal docs (via `skill-pointers.json`)

Until then, only **base scorers** run (correctness, code-quality, pr-hygiene).
